server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/victoriasuarez/config-final-victoria-suarez.git
          force-pull: true
          search-paths: config-data